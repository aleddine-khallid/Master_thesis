[project]
name = "althesis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "linearmodels>=7.0",
    "statsmodels>=0.14.6",
]
